import logging

Logging.hasicConfig(vel-logging.DEBUG,ilenames Logs.log, filemode
formate "We have next Logging message: (asctime)s:%(levelhame)s
(nessage)s"
try:
    print(18/8)
except Exception:
    logging.exception("Exception)